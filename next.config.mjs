/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "encrypted-tbn3.gstatic.com", "encrypted-tbn0.gstatic.com", "encrypted-tbn1.gstatic.com"],
    },
};

export default nextConfig;
